import { Component } from '@angular/core';

@Component({
  selector: 'app-home', // Component's selector used in HTML
  templateUrl: './home.component.html', // Path to the HTML template file
  styleUrl: './home.component.css', // Path to the CSS file for styling the component
})
export class HomeComponent {}
